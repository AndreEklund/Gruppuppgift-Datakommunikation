package test;

import control.MessageServer;

import java.io.IOException;


public class test {


    public static void main(String[] args) throws IOException {
       MessageServer messageServer= new MessageServer(434);
        //Socket socket1 = new Socket("172.20.10.6",434);
        //Socket socket2 = new Socket("172.20.10.6",434);
        //User Filip = new User("Filip",new ImageIcon());
       // User Jonas = new User("Jonas",new ImageIcon());
         //ArrayList<User> arrayList = new ArrayList<>();
         //arrayList.add(Jonas);
        // Message m = new Message(kalle,arrayList,"Från kalle till Jonas", kalle.getImage());


        //ObjectOutputStream objectOutputStream = new ObjectOutputStream(socket1.getOutputStream());
      //  objectOutputStream.writeObject(new Message(new User("Jonas",new ImageIcon()),messageServer.recivers,"Hejsan", new ImageIcon()));
        //ObjectOutputStream oos = new ObjectOutputStream(socket2.getOutputStream());
        //oos.writeObject(new Message(kalle =new User("Kalle",new ImageIcon()),messageServer.recivers,"Hejsan2", new ImageIcon()));

        //objectOutputStream.writeObject(m);




    }
}
